<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/beauty"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/beauty_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beautys">
        <activity
            android:name=".WorkPhoto.AddPhotoFirebase"
            android:exported="true" />
        <activity android:name=".StartMenu.RegisterActivity" />
        <activity android:name=".Menu.HomeActivity" />
        <activity
            android:name=".StartMenu.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>